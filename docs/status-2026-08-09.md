# Status & pickup guide — 2026-08-09

**Read this + [GAMEPLAN.md](../GAMEPLAN.md) to resume work in any conversation.** Total spend: **$26.76** of $5,000 (ledger: [budget-ledger.md](budget-ledger.md)). Test suite: 325+ green (`.venv/bin/pytest -q`).

## Where things stand (one paragraph)

The measurement stack is complete and validated: three benchmarks (DraftBench/BreakoutBench/CalibBench) with bootstrap CIs, canary gates, normalized League-Index/title-odds scoring, a leaderboard spanning GBDT → 9B/35B Qwen → DeepSeek → Claude Fable 5, and the honest headline finding — **no model breaks the base-rate "par" on historical selection (information ceiling, proven), while memorization masquerading as skill is caught by masked/canary controls (+492 named-vs-masked for Fable)**. One real signal found: **week-1 Vegas implied team totals are additive over naive stats** (era-growing, +0.23 incremental r). **T0 training smoke test: COMPLETE** — prime-rl LoRA SFT end-to-end on a pod (loss 3.05→1.31, checkpoints/resume/generation verified, $7.98, lessons in [training/t0b-status.md](../training/t0b-status.md), viz in training/t0-progress.html). The GO-checklist for real training is fully closed (canaries ✅ masked DraftGym reward ✅ survivor-bias-proof SFT filter ✅).

## Next steps, in order

1. **Monday (Aug 10–11): base-model decision** — assess the Qwen 3.8 open-weights drop (3.8-Max 2.4T is NOT a target; rumored small variants are; fallback Qwen3.5 dense small; risk register says lean dense). Write the memo, pick B2.
2. **T1 SFT (~$300 — warn Jacob first, per the $50 rule)** — run per [training/t1-run-template.md](../training/t1-run-template.md) on the T1 corpus (`data/processed/sft/t1_corpus_v1.jsonl` — generation may still be finishing; check it exists and its filter report). Season split: train 2008–2022 excluding 2013+2018 (era diagnostics), val 2023–24, 2025 sealed. Every checkpoint: eval + append to training/checkpoints_log.jsonl → dots appear on evals/results/dashboard.html.
3. **T2 RL pilot ($1–1.5k, eval-gated)** — DraftGym via envs/verifiers_v1_adapter.py, MASKED boards, realistic-manager reward, kill gates per [run-monitoring.md](run-monitoring.md). Only if T1 moved benchmarks.
4. **App un-pause (~3 days, was paused by Jacob)** — working draft advisor on real 2026 data; needs to be live ~Aug 15–17 for draft season. Spec exists (the paused app-builder agent prompt + DESIGN.md surface plan).
5. **Ledger freeze Sep 4** — 2026 predictions timestamped before kickoff (docs/breakoutbench-design.md §7). The only external proof that counts.
6. Smaller: Odds API free-key signup (unblocks daily line capture) · $30/mo props decision by ~Sep 1 · StartWho permission email · rotate the pasted API keys after launch week.

## Open items / loose ends

- Vegas-vs-ADP test agent and T1-corpus agent may still be mid-flight — check `data/processed/sft/t1_corpus_v1.jsonl` and `evals/results/vegas_vs_adp.md`; their specs are recoverable from this doc's history if they died (both resumable).
- Claude org monthly spend limit killed agents once (2026-08-09); if agents die with that error, Jacob's admin must raise it.
- v1 source-alpha lexicon should replace packet_features' looser camp flags before re-cutting news features.
- FantasyPros ECR historical + RotoWire license: launch-time decisions.
- Preseason win-totals history (P2 cross-check for the validated implied-totals channel): sportsoddshistory.com is now a JS app under covers.com (agent-stall cause); Wayback static archive has the landing page but sparse per-year (`?y=`) coverage — CDX query pending. CDX verdict: 200 distinct archived URLs exist under `nfl-win/*` (query hit its limit — likely more), so a Wayback harvest is feasible — queue as a P2 agent task (enumerate CDX urlkeys, fetch static snapshots, parse per-season tables). Not launch-blocking.

## Map

`GAMEPLAN.md` operating doc · `CLAUDE.md` rules · `docs/benchmarks-review-guide.md` how benchmarks work · `docs/benchmark-results-complete.md` all results + consumer meaning · `docs/training-risk-register.md` + `docs/run-monitoring.md` training safety · `evals/results/` every report · `harness/` engine + evidence store · `envs/` DraftGym · `training/` SFT pipeline + T0 artifacts · `scripts/` collectors (archiver runs nightly).
